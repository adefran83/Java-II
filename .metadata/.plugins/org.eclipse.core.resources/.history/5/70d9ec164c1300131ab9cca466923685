import java.awt.BorderLayout;
import java.awt.EventQueue;

import javax.swing.JFrame;
import javax.swing.JPanel;
import javax.swing.border.EmptyBorder;
import java.awt.GridLayout;
import java.awt.FlowLayout;
import javax.swing.border.BevelBorder;
import javax.swing.border.EtchedBorder;
import javax.swing.JRadioButton;
import javax.swing.JLayeredPane;


public class PizzaOrder extends JFrame {

	private JPanel contentPane;

	/**
	 * Launch the application.
	 */
	public static void main(String[] args) {
		EventQueue.invokeLater(new Runnable() {
			public void run() {
				try {
					PizzaOrder frame = new PizzaOrder();
					frame.setVisible(true);
				} catch (Exception e) {
					e.printStackTrace();
				}
			}
		});
	}

	/**
	 * Create the frame.
	 */
	public PizzaOrder() {
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		setBounds(100, 100, 450, 300);
		contentPane = new JPanel();
		contentPane.setBorder(new EmptyBorder(5, 5, 5, 5));
		contentPane.setLayout(new BorderLayout(0, 0));
		setContentPane(contentPane);
		
		JPanel Outside = new JPanel();
		contentPane.add(Outside, BorderLayout.CENTER);
		Outside.setLayout(null);
		
		JPanel InfoPanel = new JPanel();
		InfoPanel.setBorder(new EtchedBorder(EtchedBorder.LOWERED, null, null));
		InfoPanel.setBounds(12, 123, 207, 126);
		Outside.add(InfoPanel);
		InfoPanel.setLayout(new FlowLayout(FlowLayout.CENTER, 5, 5));
		
		JLayeredPane PizzaSize = new JLayeredPane();
		PizzaSize.setBorder(new EtchedBorder(EtchedBorder.LOWERED, null, null));
		PizzaSize.setBounds(12, 0, 207, 126);
		Outside.add(PizzaSize);
		PizzaSize.setLayout(new GridLayout(0, 1, 0, 0));
		
		JRadioButton rdbtnSmallPizza = new JRadioButton("Small ($8.00)");
		PizzaSize.add(rdbtnSmallPizza);
		
		JRadioButton rdbtnMediumPizza = new JRadioButton("Medium ($10.00)");
		PizzaSize.add(rdbtnMediumPizza);
		
		JRadioButton rdbtnLargePizza = new JRadioButton("Large ($12.00)");
		PizzaSize.add(rdbtnLargePizza);
		
		JRadioButton rdbtnXLargePizza = new JRadioButton("Extra-Large ($14.00)");
		PizzaSize.add(rdbtnXLargePizza);
		
		JPanel PizzaToppings = new JPanel();
		PizzaToppings.setBorder(new EtchedBorder(EtchedBorder.LOWERED, null, null));
		PizzaToppings.setBounds(220, 0, 206, 126);
		Outside.add(PizzaToppings);
		
		JPanel SummaryPanel = new JPanel();
		SummaryPanel.setBorder(new EtchedBorder(EtchedBorder.LOWERED, null, null));
		SummaryPanel.setBounds(220, 123, 206, 126);
		Outside.add(SummaryPanel);
	}
}
